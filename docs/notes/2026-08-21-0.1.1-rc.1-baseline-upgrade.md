# 2026-08-21 — rc.1 基线升级（fork `v0.1.1-rc.1+zw.1`）

上游发布 `dsh-v0.1.1-rc.1`。本次把桌面 runtime 从 `v0.1.0-rc.8+zw.4` 钉到
`v0.1.1-rc.1+zw.1` @ `751aafc710`。桌面发 `0.2.0-rc.13`。

## 上游带过来的（桌面侧要不要跟）

- DeepSeek 视觉模型 `DeepSeek-V4-Flash-Vision-Exp` —— 不碰桌面代码。
- 凭证 record API + 新包 `dsh-authorization` —— **必须改**：mcp-settings 测试里的
  `MemoryCredentials` 要补 5 个 abstract；生产路径 `credentials.resolve()` 不用动。
- 静态资源未命中改成空 **404**；boot 注入改成 `webserver/index-inject` —— 桌面没有 History
  API pathname 依赖，404 不影响。sidecar 就绪探测仍是 `GET /`（index 仍 200）。
- Composer `@` 引用前编辑布局、Bubblewrap `/proc/<pid>/root` 逃逸 —— 不碰。
- Markdown 表格自适应、99.x% 缓存命中精度、子代理标题切换、`ask_user_question` 多行 —— 不碰。

## Fork 仍保留

WKWebView `Content-Length`（index + bundle 的 200；未命中跟上游 404）、MCP
`mcp-client/status`、会话并发写守护、todo-completion-guard、模型推理档位记忆、`web:log`。
`FORK_MODIFIED` 仍是那 10 个包，`prepare-runtime.mjs` 不用改。

## 桌面改动

1. `runtime/revision.json` → `v0.1.1-rc.1+zw.1` / `751aafc710510f048e79c046e3edc790b57b5dec`。
2. 插件 `@deepseek-ai/dsh-*` devDep 一律钉 `0.1.1-rc.1`（`source-deps.mjs` REGISTRY 同步）。
3. `dsh-mcp-settings` / `dsh-desktop-bridge` 提交态里的 `link:` 恢复 registry（tsconfig
   project references / 桥 `dsh` 锚仍是遗留，不在这次迁完）。
4. `MemoryCredentials` 空实现 record API，只为过 typecheck。
5. 桥 vitest 把 `dsh-client-ui-primitives` 列入 `server.deps.inline`：registry 包带
   `.module.css`，source `link:` 时走 Vite 管道所以以前看不出来。
6. mcp-settings 监听 `credentials/reference-updated`（rc.1 把 `credentials/updated` 拆成
   reference/record 两条；生产 `resolve()` 路径没变）。

peer `>=0.1.0-rc.8 <0.2` 已经能吃到 rc.1，未改。
