# Shared Host gateway, the globally-injected model Tool (gated live by the
# `dsh-thread` settings namespace — Agent presets no longer opt in), plus the
# global browser surface.
- insert:
    - id: dsh-thread-gateway
      name: dsh-thread/gateway
    - id: dsh-thread-tool
      name: dsh-thread/tool
    - id: dsh-thread
      name: dsh-thread
